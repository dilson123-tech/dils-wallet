[build]
builder = "NIXPACKS"
buildCommand = "true"

[deploy]
startCommand = "bash backend/start.sh"
healthcheckPath = "/healthz"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"

[service.dils-wallet2]
root = "backend"
envs = [
  "DATABASE_URL",
  "JWT_SECRET",
  "HEALTH_TOKEN",
  "OPENAI_API_KEY",
  "OPENAI_BASE_URL",
  "OPENAI_MODEL",
  "IA2_ENABLED"
]
