name: Slack Webhook Test
on:
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send test to Slack
        env:
          WEBHOOK: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          set -euo pipefail
          curl -s -X POST -H 'Content-type: application/json' \
            --data '{"text":"✅ Slack webhook OK via GitHub Actions"}' \
            "$WEBHOOK"
