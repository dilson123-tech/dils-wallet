# Dependências EXCLUSIVAS de teste — nunca fazem parte do grafo de
# dependências de produção (backend/requirements.txt /
# backend/constraints-prod.txt). Instaladas somente em jobs de CI que
# rodam pytest (ex.: tests/test_pix_send_concurrency_postgres.py).
#
# packaging não é redeclarado aqui: já é dependência transitiva de
# produção pinada em constraints-prod.txt (packaging==26.3), reutilizada
# via -c abaixo.
-c constraints-prod.txt
pytest==8.3.4
pluggy==1.5.0
iniconfig==2.0.0
